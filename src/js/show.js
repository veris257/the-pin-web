
const $pinImg = document.querySelector('.grid-gallery__item')
$pinImg.addEventListener('click', (event) => {
    const $clickedPin = event.currentTarget
    openModal($clickedPin)
})

function openModal(img) {
    
    
}
